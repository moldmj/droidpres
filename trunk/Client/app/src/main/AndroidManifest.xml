<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.droidpres"
    android:versionCode="36"
    android:versionName="1.0" >

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-sdk android:minSdkVersion="8"/>
    <application
        android:description="@string/app_description"
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".activity.MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ClientListActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/lb_clitent_list" />
        <activity
            android:name=".activity.ProductListActivity"
            android:configChanges="orientation|keyboardHidden" />
        <activity
            android:name=".activity.CharacteristicListActivity"
            android:configChanges="orientation|keyboardHidden" />
        <activity
            android:name=".activity.DocumentListActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/lb_documents" />
        <activity
            android:name=".activity.SetupRootActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/lb_setup_root" />
        <activity
            android:name=".activity.SetupActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/lb_setup" />
        <activity
            android:name=".activity.TransferActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/lb_transfer" />
        <activity
            android:name=".activity.TableActivity"
            android:configChanges="keyboardHidden"
            android:label="Location" />
        <activity
            android:name=".activity.InputQtyActivity"
            android:configChanges="keyboardHidden"
            android:theme="@android:style/Theme.Dialog" />

        <receiver
            android:name=".receiver.OnBootReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.LocationService"
            android:enabled="true" />
    </application>

</manifest>